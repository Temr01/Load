<Page x:Class="Load.Specialnosts"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Load"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Specialnosts" FontSize="18" Width="910" Height="650" Loaded="Page_Loaded">

    <Grid>
        <StackPanel>
            <DataGrid x:Name="dataSpec" Margin="15" Height="610" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Код" Binding="{Binding code}" Width="180"/>
                    <DataGridTextColumn Header="Специальность" Binding="{Binding name}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Page>
